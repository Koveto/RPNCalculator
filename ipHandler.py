"""
ipHandler.py
Kobe Goodwin 12/31/2024
Call function based off input
"""



def interpretPress(PCB,BUTTON_LIST,KEYPAD_LIST,BUTTON_PINS,\
                   ip,buttons,buttonsDirect,keys,lcdScreen):
    if (PCB):
        pass
    else:
        if (ip == BUTTON_LIST[0][0]): # "A"
            pass
        if (ip == BUTTON_LIST[0][1]): # "B"
            pass
        if (ip == BUTTON_LIST[0][2]): # "C"
            pass
        if (ip == BUTTON_LIST[1][0]): # "D"
            pass
        if (ip == BUTTON_LIST[1][1]): # "E"
            pass
        if (ip == BUTTON_LIST[1][2]): # "F"
            pass
        if (ip == BUTTON_LIST[2][0]): # "G"
            pass
        if (ip == BUTTON_LIST[2][1]): # "H"
            pass
        if (ip == BUTTON_LIST[2][2]): # "I"
            pass
        if (ip == KEYPAD_LIST[0][0]): # "1"
            pass
        if (ip == KEYPAD_LIST[0][1]): # "2"
            pass
        if (ip == KEYPAD_LIST[0][2]): # "3"
            pass
        if (ip == KEYPAD_LIST[1][0]): # "4"
            pass
        if (ip == KEYPAD_LIST[1][1]): # "5"
            pass
        if (ip == KEYPAD_LIST[1][2]): # "6"
            pass
        if (ip == KEYPAD_LIST[2][0]): # "7"
            pass
        if (ip == KEYPAD_LIST[2][1]): # "8"
            pass
        if (ip == KEYPAD_LIST[2][2]): # "9"
            pass
        if (ip == KEYPAD_LIST[3][0]): # "*"
            pass
        if (ip == KEYPAD_LIST[3][1]): # "0"
            pass
        if (ip == KEYPAD_LIST[3][2]): # "#"
            pass
        if (ip == len(BUTTON_PINS) - 4): # 0
            pass
        if (ip == len(BUTTON_PINS) - 3): # 1
            pass
        if (ip == len(BUTTON_PINS) - 2): # 2
            pass
        if (ip == len(BUTTON_PINS) - 1): # 3
            pass
